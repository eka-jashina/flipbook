# Book Reader — Real Page Flip

Интерактивный веб-ридер с реалистичной анимацией перелистывания страниц, имитирующий физическую книгу. Проект реализует полноценную модель «книга → страницы → главы» без внешних библиотек, используя нативные возможности браузера (HTML, CSS, JavaScript).

---

## Назначение проекта

Проект решает задачу комфортного чтения длинных текстов в вебе с эффектом «настоящей книги»:

- визуально — 3D-книга с обложкой, корешком, страницами и тенями;
- функционально — пагинация, главы, оглавление, сохранение прогресса;
- технически — без canvas/WebGL, без фреймворков, с упором на CSS и DOM.

Подходит для:
- онлайн-книг и новелл;
- интерактивных лонгридов;
- цифровых архивов и изданий;
- экспериментальных UI/UX-проектов.

---

## Основные возможности

### 📖 Чтение и навигация
- Реалистичное перелистывание страниц (вперёд / назад).
- Открытие и закрытие книги с анимацией обложки.
- Поддержка:
  - кнопок,
  - кликов по книге,
  - клавиатуры,
  - свайпов (mobile).
- Оглавление с переходом к главам.
- Кнопка «Продолжить чтение» с восстановлением позиции.

### 🧠 Пагинация
- Автоматическая нарезка HTML-контента на страницы.
- Колоночная верстка через CSS Columns.
- Корректное начало глав с левой страницы (desktop).
- Адаптация количества страниц за перелистывание:
  - 2 страницы — desktop,
  - 1 страница — mobile.

### 🎨 Визуальная часть
- 3D-эффект книги:
  - корешок,
  - глубина страниц,
  - тени и подсветка бумаги.
- Анимированный «лист» (sheet) для перелистывания.
- Фон, привязанный к главам, с плавным cross-fade.
- Темы:
  - светлая,
  - тёмная,
  - чёрно-белая.

### ⚙️ Настройки чтения
- Размер шрифта (с ограничениями min/max).
- Выбор гарнитуры.
- Переключение темы.
- Сохранение всех настроек и текущей страницы в `localStorage`.

### ♿ Доступность
- ARIA-атрибуты для ключевых элементов.
- Поддержка клавиатурной навигации.
- Корректная работа при отключённых анимациях.

---

## Архитектура

### Ключевые компоненты

#### `BookReader`
Основной класс приложения. Управляет:
- состоянием книги;
- пагинацией;
- анимациями;
- событиями;
- настройками пользователя.

#### `BackgroundManager`
Управляет фоновыми изображениями глав:
- двойной слой (`bg-a` / `bg-b`);
- кросс-фейд без мигания.

#### `Storage`
Абстракция над `localStorage`:
- загрузка;
- сохранение;
- защита от ошибок.

#### `ErrorHandler`
Централизованная обработка ошибок:
- логирование;
- пользовательские уведомления.

---

## Применённые подходы и причины выбора

### 1. CSS + DOM вместо Canvas/WebGL
- Контент остаётся доступным для:
  - SEO;
  - screen readers;
  - выделения текста.
- Проще кастомизировать стили.
- Нет зависимости от GPU и сложных API.

### 2. Буфер страниц (double buffering)
- Используются активные и буферные страницы.
- Позволяет:
  - не перерисовывать DOM во время анимации;
  - избежать layout thrashing;
  - обеспечить плавность перелистывания.

### 3. Отдельный «sheet» для анимации
- Реальные страницы не вращаются.
- Вращается копия (лист):
  - проще контролировать `z-index`;
  - меньше побочных эффектов;
  - стабильнее на mobile.

### 4. Пагинация через CSS Columns
- Браузер сам рассчитывает переносы строк.
- Нет ручных расчётов высоты текста.
- Корректная работа при смене шрифта и размера.

### 5. Явное управление состояниями
- Флаги `busy`, `flipLock`, `inputLocked`.
- Защита от:
  - двойных кликов;
  - конфликтующих анимаций;
  - race conditions.

---

## Ограничения текущей версии

- Контент загружается из статических HTML-файлов.
- Нет виртуализации страниц (всё держится в памяти).
- Нет API для внешнего управления.

---

## Планы развития

Проект изначально проектировался с прицелом на вынос в отдельную библиотеку.

Планируется:
- 📦 Выделение в standalone-библиотеку.
- 🔌 Публичный API:
  - `open()`, `close()`;
  - `goToPage()`, `goToChapter()`;
  - события (`onFlip`, `onChapterChange`).
- 🧩 Конфигурация через опции, а не хардкод.
- 🎨 Кастомизация тем и анимаций.
- ⚡ Оптимизация загрузки и виртуализация страниц.

---

## Статус

Экспериментальный, но функционально завершённый прототип.  
Используется как proof-of-concept и основа для будущей библиотеки.